{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Truffle's `mocha` conflicts with NestJS's `jest` definitons.
    "skipLibCheck": true
  },
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "jest.config.ts"]
}
